<IfModule mod_rewrite.c>
    RewriteEngine On

    # Subdominio bahiapadel2 usa document root propio; no redirigir esa carpeta
    RewriteRule ^bahiapadel2(/|$) - [L]

    # Hostinger: document root = public_html → servir desde public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
